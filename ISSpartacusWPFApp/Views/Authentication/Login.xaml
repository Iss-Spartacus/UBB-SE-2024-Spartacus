<Window x:Class="ISSpartacusWPFApp.Views.Authentication.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ISSpartacusWPFApp.Views.Authentication"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Email:" Margin="10"/>
        <TextBox Grid.Row="0" x:Name="txtEmail" Margin="10" VerticalAlignment="Center"/>

        <Label Grid.Row="1" Content="Password:" Margin="10" />
        <PasswordBox Grid.Row="1" x:Name="txtPassword" Margin="10" VerticalAlignment="Center"/>

        <Button Grid.Row="2" Content="Login" Margin="10" Click="Login_Click"/>
        <TextBlock Grid.Row="3" x:Name="txtMessage" Margin="10" Foreground="Red"/>
    </Grid>
</Window>
